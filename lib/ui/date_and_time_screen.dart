import 'package:flutter/material.dart';

class DateAndTimeScreen extends StatelessWidget {
  const DateAndTimeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
        body: const Text('Date and time Screen')
    );
  }
}